simulator:
  upload_url: "http://127.0.0.1:8080/legacy/"
  repository_url: "http://127.0.0.1:8080/simple/"

# Paths relative to the injector directory
dataset:
  malicious_dir: "../data/samples/malicious"   # Backstabber's Knife Collection
  benign_dir: "../data/samples/benign"          # Real safe package updates

# twine credentials for the simulator (no real auth needed)
credentials:
  username: "__token__"
  password: "sim-token"

logging:
  file: "logs/injector.log"
  level: "INFO"
  console_output: true
